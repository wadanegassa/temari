# Copy to `.env` in the project root and fill in (`.env` is gitignored).
# Mobile release builds: keep secrets out of git — use CI secrets + dart-define,
# or merge keys into `assets/dotenv` only on build machines.

GEMINI_API_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=
